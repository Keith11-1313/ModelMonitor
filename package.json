{
  "name": "modelmonitor",
  "version": "1.0.0",
  "description": "LLM history and live model intelligence dashboard",
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-models.js",
    "test": "node --test scripts/*.test.js",
    "lint": "node scripts/lint.js",
    "validate": "node scripts/validate-data.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@babel/core": "^7.29.7"
  }
}
